{
  "name": "minimakers",
  "version": "1.0.0",
  "scripts": {
    "build": "npx @tailwindcss/cli@next -i ./assets/css/input.css -o ./assets/css/styles.min.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "next",
    "tailwindcss": "next"
  }
}